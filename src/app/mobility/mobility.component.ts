import { Component, OnInit } from '@angular/core';

@Component({
    selector: 'mobility-cmp',
    moduleId: module.id,
    templateUrl: 'mobility.component.html'
})

export class MobilityComponent implements OnInit{
    ngOnInit(){
    }
}
